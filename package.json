{
  "name": "futon-folding-direction",
  "description": "",
  "version": "0.1.0",
  "author": "ootidea",
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.11.6",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.2.1",
    "typescript": "^4.2.4"
  },
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "run-p esbuild typeCheck",
    "esbuild": "node esbuild.js",
    "format": "prettier --write \"src/**/*.{ts,tsx}\"",
    "typeCheck": "tsc --noEmit"
  }
}
